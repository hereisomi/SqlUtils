# SqlUtils
sqlutilz is a lightweight Python package designed for generating and executing SQL queries in a headless backend, optimized for Flask applications. It provides a secure, modular framework for building dialect-specific SQL queries from JSON payloads or Pandas DataFrames and executing them with robust connection management
